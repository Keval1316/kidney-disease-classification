services:
  - type: web
    name: nephroscan-ai
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: TF_ENABLE_ONEDNN_OPTS
        value: "0"
